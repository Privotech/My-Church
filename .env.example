# Database connection URL for PostgreSQL (Neon) and Prisma
DATABASE_URL="postgresql://user:password@host/neondb?sslmode=require"

# Neon CLI API key for headless authentication and deployment (neon deploy)
NEON_API_KEY=

